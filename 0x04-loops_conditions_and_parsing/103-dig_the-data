#!/bin/bash

# Apache log file
logfile="apache-access.log"

# Check if the log file exists
if [ ! -f "$logfile" ]; then
    echo "File $logfile not found!"
    exit 1
fi

# Use awk to parse the log file and sort the results
awk '{print $1, $9}' $logfile | sort | uniq -c | sort -nr
